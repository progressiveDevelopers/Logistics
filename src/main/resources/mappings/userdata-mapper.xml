<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.numberONe.mapper.UserDataMapper">
	<!--mybatis ehcache缓存配置 -->
	<!-- 以下两个<cache>标签二选一,第一个可以输出日志,第二个不输出日志 <cache type="org.mybatis.caches.ehcache.LoggingEhcache" 
		/> -->
	<!-- <cache type="org.mybatis.caches.ehcache.EhcacheCache"/> -->
	<!-- 以下与实体类的中字段一致 -->
	
    <resultMap type="com.numberONe.entity.UserData" id="UserData">
        <id column="id" property="ids"/>
        <result column="name" property="name"/>
        <result column="age" property="age"/>
        <result column="flag" property="flag"/>
    </resultMap>
    
    <resultMap type="com.numberONe.util.UserRelativeTreeUtil" id="userRelativeTree">
        <result column="id" property="ids"/>
        <result column="name" property="name"/>
        <result column="groupName" property="name"/>
        <result column="userName" property="name"/>
    </resultMap>
    
	<select id="findAll" resultMap="UserData">
		select id,`name`,age,flag from ly_user_data
	</select>
    
    <select id="findAllGroup" resultMap="userRelativeTree">
        SELECT g.id,g.groupName FROM ly_group g
    </select>
    
    <select id="findTargetRole" resultMap="userRelativeTree">
        SELECT t.id,t.`name`  FROM role_info t
        WHERE t.groupId = #{id}
    </select>
    
    <select id="findTargetUser" resultMap="userRelativeTree" parameterType="map">
        SELECT t.id,t.userName FROM user_info t 
        WHERE t.groupId = #{0} AND t.roleId = #{1}
    </select>
	
</mapper>